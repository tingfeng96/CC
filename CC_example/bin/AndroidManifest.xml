<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sse.ustc.cc_example"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="14" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="sse.ustc.cc.start.StartActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="sse.ustc.cc.main.MainActivity" >
        </activity>
       
        <activity android:name="sse.ustc.cc.main.MyInfoSettingActivity" >
        </activity>
        <activity android:name="sse.ustc.cc.main.ShareActivity" >
        </activity>
        <activity android:name="sse.ustc.cc.main.CourseActivity" >
        </activity>

        
    </application>

</manifest>